import React from 'react';
import "./Title.css"
const Title = () => {
    return (
        <div className="container">
            <h1 className="title text-center sticky-top">Combined Health Care</h1>
        </div>
    );
};

export default Title;